# Colour Thesaurus

Pick a colour, get its name. Type a word, get its colour.

A tiny offline-first PWA for finding the words hiding inside a hex code — or the hex code hiding inside a word.

Package name: `color-trickser`.
